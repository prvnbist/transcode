import styled from 'styled-components'

const Title3 = styled.h3`
	font-weight: 100;
	font-size: ${props => `${props.theme.basePt * 3}px`};
	padding: ${props => `${props.theme.basePt * 3}px 0`};
`

export default Title3
